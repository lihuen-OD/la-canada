/**
 * Reexporta la normalización de username desde `src/utils/` — fuente única
 * de verdad, compartida con el login (Etapa 3B.1). No se duplica la
 * implementación acá.
 */
export { normalizeUsername } from '../../src/utils/username';
